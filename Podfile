# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'OMapperTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OMapperTest
    pod 'ObjectMapper', '~> 3.4'
    pod 'HandyJSON', '~> 5.0.0'
  # pod 'IBAnimatable'
  # pod "Macaw", "0.9.5"

end
